import React from 'react';
import Nav from '../Nav/Nav.js';

const HeaderExample = () => {

    return (
        <header>
            <Nav />
        </header>
    );
};

export default HeaderExample;